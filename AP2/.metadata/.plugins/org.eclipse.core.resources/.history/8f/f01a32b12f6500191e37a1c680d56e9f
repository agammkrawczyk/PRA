/**
 *
 *  @author KRAWCZYK AGNIESZKA PD2914
 *
 */

package zad2;

import java.awt.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import javax.swing.border.*; 
import javax.swing.*;
public class Main {

  public static void main(String[] args) {
	  
	  class guzik extends JFrame {
		    JButton button;
		    int colorKey = 0;
		    Color kolorTla[] = {Color.BLUE, Color.GREEN,Color.gray,Color.CYAN, Color.ORANGE} ;	
		   
		    guzik() {
		        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		        setPreferredSize(new Dimension(200, 100));
		        setLocation(200, 200);
		
		        button = new JButton("Klik!");
		        
		
		        button.addActionListener(new ActionListener() {
		            @Override
		
		            public void actionPerformed(ActionEvent e) {
		                if (colorKey >= kolorTla.length) {
		                    colorKey = 0;
		                }
	              button.setBackground(kolorTla[colorKey]);
		                colorKey++;
		            }
		        });
		        add(button);
		
		        pack();
		        setVisible(true);
		    }}
	
	      SwingUtilities.invokeLater(new Runnable() {
	          public void run() {
	              new guzik();
	          }
	      });
	  }
}  
	  
	 

